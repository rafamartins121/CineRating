@model IEnumerable<CineRating.Models.Atores>

@*@{
        ViewBag.Title = "Index";
    }*@


@using (Html.BeginForm()) {
    <div style="text-align:center">
        @Html.TextBox("pesq", "", new { @id = "nomeAtor", @autocomplete = "off", @placeholder = "Pesquisar Atores", style = "width:500px; height:35px;" })
        <input type="submit" class="btn btn-default" value="Pesquisar" style="display:none;" />
    </div>
}


@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataNascimento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Biografia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Imagem)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataNascimento)
            </td>z
            <td>
                @Html.DisplayFor(modelItem => item.Biografia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Imagem)
                <a href="~/imagens/atores/@item.Imagem" target="_blank">
                    <img src="~/imagens/atores/@item.Imagem" height="150" />
                </a>
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }

    </table>*@

<div style="display:flex; flex-direction:row; flex-wrap:wrap;">
    @foreach (var item in Model) {
        <div style="text-align:center">
            <br />
            <p>@Html.DisplayFor(modelItem => item.Nome)</p>
            <a href="@Url.Action("Details", "Atores", new { id = item.ID })">
                <img src="~/imagens/atores/@item.Imagem" width="250" style="margin:15px; object-fit:cover;" />
            </a>
        </div>
    }
</div>



<link href="~/Content/themes/base/jquery-ui.css"
      rel="stylesheet"
      type="text/css" />


@section Scripts {
    <script src="~/Scripts/jquery-ui-1.12.1.min.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {
            $('#nomeAtor').autocomplete({
                source: '/Atores/GetAtores'
            });
        });
    </script>
}

